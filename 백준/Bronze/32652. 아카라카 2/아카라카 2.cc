#include <stdio.h>
#include <string.h>

int main() {
    int K;
    scanf("%d", &K);

    printf("AKARAKA");
    for (int i = 1; i < K; i++) {
        printf("RAKA");
    }
    printf("\n");

    return 0;
}