#include <stdio.h>

int main() {
    char name[51];
    scanf("%s", name);
    printf(":fan::fan::fan:\n");
    printf(":fan::%s::fan:\n", name);
    printf(":fan::fan::fan:\n");
    return 0;
}