#include <stdio.h>

int main(void) {
    int m;
    int n;
    int t;

    scanf("%d", &m);
    scanf("%d", &n);
    scanf("%d", &t);

    int total_distance = m * 2;

    printf("%d\n", total_distance);

    return 0;
}

